from django.test import TestCase

"""Placeholder tests for the `core` app.

Add unit tests here for model behavior, serializers, and management
commands. Start with tests for `sync_game_from_steam()` (success and
failure), `GameDetailSerializer` output, and the `sync_game` command.
"""


class PlaceholderTests(TestCase):
	def test_placeholder(self):
		# Intentionally trivial test so `manage.py test` runs until real
		# tests are implemented.
		self.assertTrue(True)
